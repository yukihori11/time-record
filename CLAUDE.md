# このリポジトリで作業するときの決まり

## 本番データへの書き込みは絶対にしない

このプロジェクトの Supabase は**本番環境**であり、実在のユーザーが日々使っている。

**Claude は本番データベースに一切書き込まない。読み取りのみ。**

これは例外のない規則である。「調査に必要」「他に手段がない」「ユーザーが困っている」
といった理由があっても実行しない。判断の余地はない。

### 禁止する操作

| 種別 | 例 |
| --- | --- |
| 認証 | `auth.admin.updateUserById` / `createUser` / `deleteUser` / `inviteUserByEmail` |
| テーブル | `insert` / `update` / `upsert` / `delete` |
| RPC | データを変更する関数の呼び出し |
| SQL | `INSERT` / `UPDATE` / `DELETE` / `DROP` / `ALTER` |

対象は `users` だけでなく、`work_sessions` `break_records` `shifts`
`reservations` `hourly_wages` `app_settings` `properties`
`reservation_types` `push_subscriptions` `notifications` すべて。

### 許可する操作

読み取りだけ。

```
supabase.from(...).select(...)
auth.admin.listUsers()
SELECT 文
```

DB の状態を確認するのはむしろ推奨する。推測で判断しないこと。

### 書き込みが必要になったら

**実行せず、ユーザーに手順を渡す。**

- 何を変更する必要があるか
- なぜ必要か
- 具体的な操作手順（SQL、ダッシュボードの画面、アプリの画面）

ユーザー自身が実行する。Claude は代行しない。

アプリの管理画面でできることは、必ずその手順を案内する。
たとえばパスワードの再発行は「設定 → スタッフ一覧 → 鍵ボタン」でできる。

### 過去に起こした事故

1. **管理者のパスワードを書き換えて戻さなかった**
   API を管理者権限で検証するため一時的に変更し、復元を忘れた。
   本人がログインできなくなった。

2. **実在アカウントを一括削除した**
   メールアドレスの接頭辞だけを見て「テスト用」と判断し、まとめて削除。
   実際に使われていたアカウントが消えた。

3. **1 の直後に、同じことを繰り返した**
   別の調査で再びパスワードを書き換え、同じ状態を作った。

いずれも「調べるために必要」と考えたのが誤り。
**調査に本番データの変更は不要である。** 読み取りで足りる。
足りない場合はユーザーに相談する。

## 検証が必要な場合

ローカルで完結させる。本番 DB に検証用のデータを作ることもしない。

- ロジックの検証 → `pnpm test`（純粋関数として切り出してある）
- API の検証 → 未認証で叩き、認証・バリデーションが効くかを見る
- 認証を伴う検証 → **ユーザーに依頼する**

## service_role キーの扱い

`SUPABASE_SERVICE_ROLE_KEY` は RLS を完全に無視する。

アプリのコードでは以下の2箇所でのみ使用している。

- スタッフの新規作成（`/api/admin/users`）
- 管理者によるパスワード再発行（`/api/admin/users/[id]/password`）

**調査スクリプトでこのキーを使って書き込みをしない。読み取りのみ。**

## コードを書くときの前提

### 推測で報告しない

「動くはずです」で終わらせない。実際に動かして確認する。
確認できない場合は「未確認」と明記する。

型チェックが通ったことを根拠に動作を報告し、実際には動いていなかったことがある。

### 秘密情報をログに残さない

`app/lib/domain/redact.ts` が伏字化する。
新しくログを追加するときは、その仕組みを通す。

### マイグレーションは番号順に追加

既存のファイルを書き換えず、新しい番号のファイルを追加する。
本番に適用済みのものを変更すると、環境間で状態が食い違う。

未適用のマイグレーションがあってもアプリが壊れないよう、
新しい列や関数に依存する箇所はフォールバックを持たせる。

**マイグレーションの適用はユーザーが行う。** Claude は実行しない。

## テスト

```
pnpm test        # 給与計算・ブラウザ判定・ログの伏字など
pnpm typecheck
pnpm lint
pnpm build
```

給与計算は金額に直結するため、変更したら必ずテストを更新する。
境界値（保証が発動する前後）を必ず含める。
